[project]
name = "minimind-reproduce"
version = "0.1.0"
description = "Learn to reproduce the project of minimind.  学习如何复现minimind项目。"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.3.4",
    "pandas>=2.3.3",
    "torch>=2.9.0",
    "transformers>=4.57.1",
]